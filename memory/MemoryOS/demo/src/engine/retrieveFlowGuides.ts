/** 检索流各步流程说明（右侧栏自然语言，不含代码符号） */

export interface RetrieveFlowGuide {
  title: string
  content: string
}

const GUIDES: Record<string, RetrieveFlowGuide> = {
  r_mid: {
    title: '检索流程 · 中期记忆',
    content: `1. 将用户当前问题转化成向量表示。
2. 在中期记忆里，用该向量与各 session 主题摘要的向量比对，找出主题相近的若干 session。
3. 在每个候选 session 内部，再与各条 page（一问一答）的向量比对，筛出相似度足够高的 page。
4. 汇总各 session 命中的 page，按相似度排序，取排名靠前的若干条，作为本轮可参考的「历史对话片段」。
5. 被检索命中的 session 的 N_visit 加 1，H_segment 随之重算升高，便于后续再次关注该话题。`,
  },
  r_user_k: {
    title: '检索流程 · 用户长期知识',
    content: `（与用户画像读取、助手知识检索可同时进行）

1. 将用户当前问题转化成向量表示。
2. 在用户长期知识库中，逐条与已沉淀的事实性知识比对相似度。
3. 超过门槛的条目视为与本问题相关，作为回答时的用户侧事实依据。`,
  },
  r_asst_k: {
    title: '检索流程 · 助手长期知识',
    content: `（与中期检索、用户知识检索可同时进行）

1. 将用户当前问题转化成向量表示。
2. 在助手长期知识库中检索语义相近的条目（如通用常识、领域背景等）。
3. 命中的内容供生成回答时引用，补充助手侧知识，而非用户私有信息。`,
  },
  r_short: {
    title: '检索流程 · 短期对话',
    content: `1. 直接读取短期记忆中保留的最近几轮完整问答。
2. 不经向量检索，用于保持当下对话的连贯性与即时上下文。
3. 整理成连续对话文本，稍后与用户画像、检索结果等一并交给模型。`,
  },
  r_profile: {
    title: '检索流程 · 用户画像',
    content: `1. 直接读取长期记忆中已保存的用户画像全文。
2. 画像由此前多轮对话归纳而成，描述用户较稳定的特征与偏好。
3. 作为背景信息写入提示，不走相似度检索。`,
  },
  r_prompt: {
    title: '检索流程 · 拼装提示',
    content: `将以下信息组织成发给模型的完整提示：

· 短期记忆中的最近几轮对话
· 中期检索命中的历史对话片段（含各 page 的主题摘要）
· 用户画像与检索到的用户长期知识
· 检索到的助手长期知识
· 用户当前提出的问题

系统侧提示侧重角色、助手知识与元信息；用户侧提示侧重历史、检索内容与问题本身。`,
  },
  r_generate: {
    title: '检索流程 · 生成回复',
    content: `1. 将上一步拼好的系统提示与用户提示一并交给大模型。
2. 模型综合短期上下文、中期历史片段、长期画像与知识，针对当前问题生成回答。
3. 本演示使用预设回复；真实环境中为模型实时生成。`,
  },
  r_writeback: {
    title: '检索流程 · 写回记忆',
    content: `1. 将本轮「用户提问 + 助手回答」作为新的一问一答写入短期记忆。
2. 短期未满时仅追加；短期已满时，会先把最老的对话晋升到中期，再写入本轮内容。
3. 写回后可供后续对话继续引用，并可能再次触发中期加热或长期更新。`,
  },
}

export function getRetrieveFlowGuide(stepId: string): RetrieveFlowGuide {
  return (
    GUIDES[stepId] ?? {
      title: '检索流程',
      content: '（暂无说明）',
    }
  )
}
